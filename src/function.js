"use strict";
// Normal DUnction
function multiplication(x, y) {
    const result = x * y;
    return result;
}
// Array Function
const sum = (x, y) => {
    return x + y;
};
const num1 = 5;
const num2 = 10;
console.log(multiplication(num1, num2));
console.log(sum(num1, num2));
